<div style="font-family: Arial;">
  <h2>Cotización para {{nombre_cliente}}</h2>
  <p>Gracias por tu interés. Aquí tienes los productos:</p>

  <table>
    <thead>
      <tr><th>Producto</th><th>Cantidad</th><th>Precio</th></tr>
    </thead>
    <tbody>
      {{#each productos}}
        <tr>
          <td>{{name}}</td>
          <td>{{cantidad}}</td>
          <td>${{price}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <p><strong>Total:</strong> ${{total}}</p>
</div>
